# Vercel: Project → Settings → Environment Variables (Production + Preview as needed).
# Value must be your Render API base including /api, no trailing slash, e.g.:
# VITE_API_BASE_URL=https://quicknote-backend.onrender.com/api

VITE_API_BASE_URL=
